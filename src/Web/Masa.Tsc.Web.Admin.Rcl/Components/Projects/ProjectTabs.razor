@inherits TscComponentBase

<MTabs Class="transparent-tab" Value="Value" ValueChanged="async value => await ValueChanged.InvokeAsync(value.ToString())">
    @foreach (var item in ProjectTypes)
    {
        <MTab Value=@item.Key Class="text-capitalize">@item.Value</MTab>
    }
</MTabs>