@namespace Masa.Tsc.Web.Admin.Rcl.Components
@inherits TscComponentBase
<MCard>
    @if (!ReadOnly)
    {
        <MCardTitle Class="d-flex justify-end">
            <MButton OnClick="OpenDialog">添加</MButton>
            <MButton OnClick="@(async ()=> await OnCallParent(OperateCommand.Close))">Close</MButton>
        </MCardTitle>
    }
    <MCardText>
        <MDragZone @ref="_mDragZone" Options="asdasdasd" Class="d-flex"></MDragZone>
    </MCardText>
</MCard>

<TscDialog @bind-Value="_showDialog" Persistent Fullscreen>
    <ChildContent>
        <MCard>
            @if (_editPanel == null)
            {
                <MCardTitle>
                    <MRow>
                        <MCol Cols=9>添加</MCol>
                        <MCol Cols=3 Class="d-flex justify-end">
                            <MIcon OnClick="CloseDialog" Class="justify-end">mdi-close</MIcon>
                        </MCol>
                    </MRow>
                </MCardTitle>
                <MCardText>
                    <TscInstrumentPanels InstrumentId="InstrumentId" ParentId="ParentId" CallParent="ChildCallHandler" />
                </MCardText>
            }
            else
            {
                <TscInstrumentPanelView @bind-Value="_editPanel" Readonly="ReadOnly" CallParent="ChildCallHandler" />
            }
        </MCard>
    </ChildContent>
</TscDialog>