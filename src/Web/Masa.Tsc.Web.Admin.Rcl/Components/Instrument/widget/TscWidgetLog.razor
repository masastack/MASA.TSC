@namespace Masa.Tsc.Web.Admin.Rcl.Components
@inherits TscWidgetBase

<TscLog/>

@code {
    public override AddPanelDto ToPannel()
    {
        return new AddPanelDto
            {
                Type = InstrumentTypes.Log
            };
    }
}