@namespace Masa.Tsc.Web.Admin.Rcl.Components
@inherits TscWidgetBase

@*<MTab Value="@_panelValue.Id.ToString()">
    @if (!ReadOnly)
    {
        <MTextField TValue="string" Value="_panelValue.Title" OnChange="OnNameChange" />
    }
    else
    {
        @_panelValue.Title
    }
    <MIcon Small OnClick="OnRemove">mdi-delete</MIcon>
</MTab>*@