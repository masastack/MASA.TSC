@inherits TscComponentBase

<DefaultDialog Value=Visible ValueChanged=UpdateVisible Title="@T("Update folder")">
    <ChildContent>
        <MForm @ref="Form" Model=Folder EnableValidation EnableI18n>
            <STextField Required @bind-Value="@Folder.Name" />
        </MForm>
    </ChildContent>
    <FooterContent>
        <SButton Large OnClick="UpdateFolderAsync">@T("Save")</SButton>
    </FooterContent>
</DefaultDialog>